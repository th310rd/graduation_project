server:
  port: 8089

spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/geodb}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
  application:
    name: geotracking-service

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
